// Main tsconfig for building. Prevents config scripts, previous builds, and tests from being compiled.
{
  "extends": "./tsconfig.json",
  "exclude": [
    "jest.config.js",
    "build/",
    "src/tests/",
    "**/*.spec.ts",
    "**/*.test.ts",
    "**/__tests__/",
    "**/__mocks__/",
  ]
}
